<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IMCApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/brand_accent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.IMCApp" parent="Base.Theme.IMCApp" />

    <!-- Tema de la pantalla de animación: sin barras y con fondo degradado -->
    <style name="Theme.IMCApp.Splash" parent="Theme.IMCApp">
        <item name="android:windowBackground">@drawable/bg_splash_gradient</item>
    </style>

    <!-- Estilos reutilizables -->
    <style name="TextTitle">
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">?attr/colorOnSurface</item>
    </style>

    <style name="TextBody">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">?attr/colorOnSurfaceVariant</item>
        <item name="android:lineSpacingExtra">4dp</item>
    </style>
</resources>
